import React from 'react';

const VerifySectionLayout = ({
    children
}: {
    children: React.ReactNode;
}) => {
    return (
        <>{ children }</>
    );
};

export default VerifySectionLayout;